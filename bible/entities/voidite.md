<!-- @generated by @newel/generator-bible — do not edit -->
# Voidite

**Tags:** `material`

Jet-black crystalline ore found only in void-touched biomes and deep rifts. Extremely rare, highly conductive of void-type magic; dangerously unstable in raw form.

> **Goal:** Serve as the rarest, most powerful — and most dangerous — late-game magical material

## Fields

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `id` | uuid | yes |  |
| `state` | enum (`raw`, `refined`, `enchanted`) | yes |  |
| `density` | decimal | yes | g/cm³; governs item weight |
| `hardness` | decimal | yes | Mohs-equivalent scale 1–10 |
| `conductivity` | decimal | yes | Thermal conductivity rating 0–1 |
| `magicAffinity` | decimal | yes | Capacity to hold enchantment 0–1 |

### State machine

**Field:** `state` &nbsp; **Initial:** `raw`

```mermaid
stateDiagram-v2
  [*] --> raw
  raw --> refined : refine\n[Requires Void Smithing: Expert — an esoteric skill unlocked only via experimentation; Raw voidite emits void pulses that corrupt nearby items; shielded forge room mandatory; One raw crystal yields one refined shard; failures may cause a void burst event — surviving such a burst grants the voidBurstSurvivor flag regardless of tile location (same flag as VoidRift.applyHazards; either source is sufficient)]
  refined --> enchanted : enchant\n[Only players who have survived a void burst (voidBurstSurvivor flag, granted by surviving any void burst — via VoidRift.applyHazards or Voidite.refine failure) may learn to enchant voidite; Enchanted voidite cannot be stored in standard item bags without void-lining]
  enchanted --> [*]
```

| State | Description | Terminal |
| ----- | ----------- | -------- |
| `raw` | Ore or unprocessed form; must be smelted or worked before use |  |
| `refined` | Processed ingot or sheet; ready for crafting |  |
| `enchanted` | Imbued with magical energy at an arcane forge | yes |

| From | To | Trigger | Guards | Effects |
| ---- | --- | ------- | ------ | ------- |
| `raw` | `refined` | `refine` | Requires Void Smithing: Expert — an esoteric skill unlocked only via experimentation; Raw voidite emits void pulses that corrupt nearby items; shielded forge room mandatory; One raw crystal yields one refined shard; failures may cause a void burst event — surviving such a burst grants the voidBurstSurvivor flag regardless of tile location (same flag as VoidRift.applyHazards; either source is sufficient) |  |
| `refined` | `enchanted` | `enchant` | Only players who have survived a void burst (voidBurstSurvivor flag, granted by surviving any void burst — via VoidRift.applyHazards or Voidite.refine failure) may learn to enchant voidite; Enchanted voidite cannot be stored in standard item bags without void-lining |  |

## Behaviors

### `refine`

Stabilise raw voidite through void-tuned smelting in a specially shielded forge

**Rules:**
- Requires Void Smithing: Expert — an esoteric skill unlocked only via experimentation
- Raw voidite emits void pulses that corrupt nearby items; shielded forge room mandatory
- One raw crystal yields one refined shard; failures may cause a void burst event — surviving such a burst grants the voidBurstSurvivor flag regardless of tile location (same flag as VoidRift.applyHazards; either source is sufficient)

**Auth:** `maintainer`

### `enchant`

Void-attune a refined shard to its maximum magical capacity

**Rules:**
- Only players who have survived a void burst (voidBurstSurvivor flag, granted by surviving any void burst — via VoidRift.applyHazards or Voidite.refine failure) may learn to enchant voidite
- Enchanted voidite cannot be stored in standard item bags without void-lining

**Auth:** `maintainer`



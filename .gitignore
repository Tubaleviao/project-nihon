# Godot 4+ specific ignores
.godot/
/android/
.vscode/

# Node.js (fabric tooling — @newel)
node_modules/

# Production asset pack (built from the private assets submodule — never commit)
assets.pck
tools/pack_project/_overlay/
# Added by code-review-graph
.code-review-graph/
